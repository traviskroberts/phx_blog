# PhxBlog

Running locally:

  * `mix deps.get`
  * `mix ecto.setup`
  * `cd assets && npm install`
  * `mix phx.server`

Now you can visit [`localhost:4000`](http://localhost:4000) from your browser.
